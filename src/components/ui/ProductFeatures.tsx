// const ProductFeatures = ({features}: {features: {brand?: string; model?: string; generation?: string; graphics?: string; resolution?: string}}) => {
//   const mappedObject = Object.entries(features).reduce((acc, [property, value]) => {
//     acc[`${property}`] = value;
//     return acc;
//   }, {});
//   return (
//     <div>
//       <h1>Key Features:</h1>
//     </div>
//   );
// };

// export default ProductFeatures;
